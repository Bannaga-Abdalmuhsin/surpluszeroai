[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="surpluszeroai"
version="0.1.0"
requires-python=">=3.11"
dependencies=[]
[project.scripts]
surpluszero="surpluszero.pipeline:main"
[tool.setuptools.packages.find]
where=["src"]
[tool.pytest.ini_options]
pythonpath=["src"]
